using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HighScoreDisplay : MonoBehaviour
{
    /*void OnGUI()
    {
        GUIStyle gs = new GUIStyle();
        gs.richText = true;
        gs.wordWrap = true;
        gs.normal.textColor = Color.black;

        Rect rect1 = new Rect(500, 650, 300, 200);
        Rect rect2 = new Rect(500, 700, 300, 200);

        GUI.Label(rect1, "<size=20>High Score: " + PlayerPrefs.GetInt("highScore") + "</size>", gs);
        GUI.Label(rect2, "<size=20>High Score: " + PlayerPrefs.GetInt("highScore2") + "</size>", gs);
    }*/
}
